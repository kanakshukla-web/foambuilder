import { ShapeLibFilterPipe } from './shape-lib-filter.pipe';

describe('ShapeLibFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShapeLibFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
